---
import type { JSX } from "astro/jsx-runtime";

type Props = JSX.IntrinsicElements["button"];
const props = Astro.props;
---

<button
  class="w-full px-2 py-1 mt-2 rounded-md border-none bg-slate-200 text-base"
  {...props}><slot /></button
>
