export enum Collection {
  Pages = "pages",
  Users = "users",
  Widgets = "widgets",
}

export enum Languages {
  Default = "en",
}

export enum ErrorCodes {
  PageNotFound = 1000,
  SlugAlreadyExists = 1001,
  SlugIsInvalid = 2003,
}
